# Структура плагина FS WP Multilang Integration

## Общая структура проекта

```
wp-content/plugins/fs-wpmultilang-integration/
├── admin/                      # Административные функции и интерфейс
├── examples/                   # Примеры использования плагина
├── includes/                   # Основные классы и функции плагина
├── languages/                  # Языковые файлы для локализации
├── public/                     # Публичные функции и фронтенд
├── CHANGELOG.md               # История изменений
├── LICENSE.txt                # Лицензия GPL v2
├── fs-wpmultilang-integration.php  # Главный файл плагина
├── index.php                  # Защита директорий
├── loader.php                 # Автозагрузчик классов
├── readme.txt                 # Описание плагина для WordPress
└── uninstall.php              # Скрипт удаления плагина
```

## Подробное описание каждой папки

### 📁 `admin/`
**Назначение:** Административная часть плагина
- **Расположение кода:** Файлы, связанные с админ-панелью WordPress
- **Функции:**
  - Настройки плагина
  - Метабоксы и пользовательские поля
  - Админ-меню и страницы
  - Обработка форм в админке
- **Примеры файлов для добавления:**
  ```
  admin/class-admin-settings.php     # Страница настроек
  admin/class-meta-boxes.php         # Метабоксы для постов
  admin/class-admin-notices.php      # Уведомления в админке
  ```

### 📁 `examples/`
**Назначение:** Примеры использования и демонстрационный код
- **Расположение кода:** Обучающие и тестовые примеры
- **Функции:**
  - Демонстрация API плагина
  - Примеры интеграции
  - Тестовые сценарии
- **Примеры файлов:**
  ```
  examples/example-usage.php         # Базовые примеры
  examples/advanced-examples.php     # Расширенные примеры
  examples/hooks-reference.php       # Справочник хуков
  examples/hreflang-examples.php     # Примеры работы с hreflang (см. includes/fs-wpml-hreflang-handler.php)
  ```

### 📁 `includes/`
**Назначение:** Основная бизнес-логика плагина (САМАЯ ВАЖНАЯ ДЛЯ РАСШИРЕНИЯ)
- **Расположение кода:** Ядро функциональности плагина
- **Функции:**
  - Основные классы плагина
  - Обработчики языковой логики
  - Интеграция с WP Multilang
  - Сервисные классы
- **Примеры файлов для добавления:**
  ```
  includes/class-language-router.php      # Роутинг языков
  includes/class-content-filter.php       # Фильтрация контента
  includes/class-api-handler.php          # Обработчик API
  includes/class-cache-manager.php        # Управление кэшем
  includes/class-compatibility.php        # Совместимость с темами
  includes/fs-wpml-hreflang-handler.php   # Обработка hreflang атрибутов (уже реализовано)
  ```

### 📁 `languages/`
**Назначение:** Локализация плагина
- **Расположение кода:** Языковые файлы (.po/.mo)
- **Функции:**
  - Перевод интерфейса плагина
  - Локализация сообщений
  - Мультиязычная поддержка
- **Файлы создаются автоматически:**
  ```
  languages/fs-wpmultilang-integration-ru_RU.po
  languages/fs-wpmultilang-integration-ru_RU.mo
  ```

### 📁 `public/`
**Назначение:** Публичная (фронтенд) часть плагина
- **Расположение кода:** Функции, работающие на лицевой части сайта
- **Функции:**
  - Шорткоды
  - Виджеты
  - Фронтенд скрипты и стили
  - Обработчики AJAX
- **Примеры файлов для добавления:**
  ```
  public/class-shortcodes.php        # Пользовательские шорткоды
  public/class-widgets.php           # Виджеты для сайдбара
  public/class-frontend-scripts.php  # Скрипты и стили
  public/class-ajax-handlers.php     # AJAX обработчики
  ```

## Где добавлять свой код

### 🔧 Для расширения основной функциональности:
**Добавляйте файлы в `includes/`**
```php
// Пример: includes/class-custom-feature.php
class FS_WPML_Custom_Feature {
    public function __construct() {
        add_action('init', array($this, 'initialize'));
    }
    
    public function initialize() {
        // Ваша логика здесь
    }
}
```

### 🎛️ Для административных функций:
**Добавляйте файлы в `admin/`**
```php
// Пример: admin/class-custom-settings.php
class FS_WPML_Admin_Custom_Settings {
    public function __construct() {
        add_action('admin_menu', array($this, 'add_menu'));
    }
    
    public function add_menu() {
        add_submenu_page(...); // Ваше меню
    }
}
```

### 🌐 Для фронтенд функциональности:
**Добавляйте файлы в `public/`**
```php
// Пример: public/class-custom-shortcode.php
class FS_WPML_Public_Shortcode {
    public function __construct() {
        add_shortcode('my_shortcode', array($this, 'handle_shortcode'));
    }
    
    public function handle_shortcode($atts) {
        // Логика шорткода
        return 'Ваш результат';
    }
}
```

## Как подключать новые файлы

### 1. Через автозагрузчик (рекомендуется):
Файлы автоматически загружаются если следуют конвенции именования:
- Класс `FS_WPML_Language_Handler` → файл `fs-wpml-language-handler.php`
- Класс `FS_WPML_Custom_Widget` → файл `fs-wpml-custom-widget.php`

### 2. Вручную в главном файле:
```php
// В fs-wpmultilang-integration.php
require_once FS_WPMULTILANG_INTEGRATION_PATH . 'includes/your-file.php';
require_once FS_WPMULTILANG_INTEGRATION_PATH . 'admin/your-admin-file.php';
```

### 3. Через хуки WordPress:
```php
// В функции init_integration()
private function init_integration() {
    // Подключение дополнительных файлов
    require_once FS_WPMULTILANG_INTEGRATION_PATH . 'includes/additional-features.php';
}
```

## Архитектура плагина

### Основной класс (Singleton):
```php
class FS_WPML_Integration {
    private static $instance = null;
    
    public static function get_instance() {
        if (null === self::$instance) {
            self::$instance = new self();
        }
        return self::$instance;
    }
}
```

### Система хуков:
Плагин использует стандартные WordPress хуки:
- `plugins_loaded` - загрузка текстдомена
- `init` - инициализация функциональности
- `admin_menu` - создание меню админки

## Пример расширения плагина

### Добавление нового функционала:

1. **Создайте файл в `includes/`:**
```php
// includes/class-seo-integration.php
class FS_WPML_SEO_Integration {
    public function __construct() {
        add_action('wp_head', array($this, 'add_hreflang_tags'));
    }
    
    public function add_hreflang_tags() {
        // Логика добавления hreflang тегов
    }
}
```

2. **Подключите его в основном классе:**
```php
// В методе init_integration()
private function init_integration() {
    // Существующая логика...
    
    // Новый функционал
    new FS_WPML_SEO_Integration();
}
```

## Рекомендации по разработке

### ✅ Хорошие практики:
- Используйте префикс `fs_wpml_` для функций
- Следуйте PSR стандартам кодирования
- Документируйте код комментариями
- Используйте транзиенты для кэширования
- Проверяйте зависимости через `function_exists()` или множественные методы проверки

### ❌ Избегайте:
- Прямых запросов к базе данных без `$wpdb->prepare()`
- Жестко закодированных путей
- Глобальных переменных
- Длинных функций без разделения на методы

## Отладка и тестирование

### Включение отладки:
```php
// Добавьте в wp-config.php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

### Логирование:
```php
// В ваших файлах
error_log('Debug message: ' . print_r($variable, true));
```

### Проверка зависимостей:
Плагин использует несколько методов для проверки активности WP Multilang:
1. Проверка наличия ключевых функций (`wpm_get_languages`, `wpm_get_user_language`)
2. Проверка существования основного класса с правильным namespace
3. Проверка наличия основной функции `wpm()`

Это обеспечивает надежное определение активности плагина WP Multilang.